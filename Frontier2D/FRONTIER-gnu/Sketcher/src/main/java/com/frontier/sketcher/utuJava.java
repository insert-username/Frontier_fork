package com.frontier.sketcher;

/**
 * This is where the jni solver methods were previously being called from. Since I removed that a generic interface
 * can be used instead, then connected to whatever (testing, logging, solver etc)
 */
public class utuJava {
    public void utuC(int[] dataInt, double[] dataDouble) {
        throw new IllegalStateException("Debug the UTU input here.");
    }
}
